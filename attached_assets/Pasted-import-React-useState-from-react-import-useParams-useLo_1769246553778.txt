import React, { useState } from "react";
import { useParams, useLocation } from "wouter";
import { 
  Trophy, 
  Users, 
  MapPin, 
  Clock, 
  Calendar, 
  Fish, 
  TrendingUp, 
  Activity, 
  ChevronRight,
  Target,
  Crown,
  Share2,
  AlertCircle,
  Timer,
  BarChart3,
  X,
  PieChart,
  ArrowUpRight,
  ChevronDown,
  ChevronUp,
  Mic
} from "lucide-react";

// --- MOCK DATA ---

const competition = {
  id: "1",
  name: "Zemplínsky Kapor 2026",
  status: "live", // 'registration', 'live', 'ended'
  location: "Zemplínska Šírava",
  startDate: "2026-05-15",
  endDate: "2026-05-20",
  teamsCount: 42,
  description: "Najväčší kaprársky pretek na východe. 5 dní nonstop lovu.",
  image: "https://images.unsplash.com/photo-1544551763-77ef2d0cfc6c?q=80&w=1200&auto=format&fit=crop"
};

const liveStats = {
  totalFish: 342,
  totalWeight: 1250.5,
  biggestFish: 21.4,
  avgWeight: 3.6
};

// Generujeme viac dát pre leaderboard, aby fungoval "Show more"
const leaderboard = Array.from({ length: 20 }).map((_, i) => ({
  rank: i + 1,
  name: i === 0 ? "CT Carp Hunters" : i === 1 ? "No Respect Kimot" : i === 2 ? "Fox International" : `Team ${i + 1}`,
  weight: i === 0 ? 145.2 : i === 1 ? 138.5 : 140 - (i * 2.5),
  fish: 12 - Math.floor(i / 3),
  sector: ["A", "B", "C"][i % 3],
  trend: i % 3 === 0 ? "up" : "down"
}));

const liveFeed = [
  { id: 1, team: "CT Carp Hunters", action: "catch", weight: 12.5, fish: "Kapor", time: "Pred 2 min", sector: "A" },
  { id: 2, team: "Mikbaits Team", action: "catch", weight: 8.2, fish: "Amur", time: "Pred 15 min", sector: "B" },
  { id: 3, system: true, msg: "Nočný kľud – od tejto chvíle sa nekŕmi", time: "Pred 30 min" },
  { id: 4, team: "No Respect Kimot", action: "big_fish", weight: 18.4, fish: "Kapor", time: "Pred 45 min", sector: "C" },
  { id: 5, team: "Fox International", action: "catch", weight: 5.5, fish: "Kapor", time: "Pred 1h", sector: "B" },
];

// --- STATS DATA ---
const sectorStats = [
    { name: "Sektor A (Hrádza)", weight: 450, color: "bg-cyan-500" },
    { name: "Sektor B (Prístav)", weight: 320, color: "bg-purple-500" },
    { name: "Sektor C (Zátoka)", weight: 480, color: "bg-emerald-500" }, 
];

const hourlyActivity = [
    { hour: "06:00", val: 12 },
    { hour: "09:00", val: 8 },
    { hour: "12:00", val: 4 },
    { hour: "15:00", val: 15 },
    { hour: "18:00", val: 32 }, 
    { hour: "21:00", val: 24 },
    { hour: "00:00", val: 10 },
    { hour: "03:00", val: 6 },
];

// --- LOGIC: THE COMMENTATOR BRAIN ---
// V reálnej appke by toto bola funkcia, ktorá analyzuje props
const getCommentary = (type: 'short' | 'full') => {
    // Simulovaná logika na základe Mock Dát:
    // 1. Peak je 18:00
    // 2. Sektor C vedie na váhu
    
    if (type === 'short') {
        return "Ryby sa ozývajú hlavne po 18:00.";
    }
    return "Najviac záberov prichádza večer okolo 18-tej, ale tie najťažšie ryby sa momentálne chytajú v Zátoke.";
};

// --- COMPONENTS ---

const StatusBadge = ({ status }: { status: string }) => {
    if (status === 'live') {
        return (
            <div className="flex items-center gap-2 bg-red-500/10 border border-red-500/20 text-red-500 px-3 py-1 rounded-full animate-pulse">
                <span className="relative flex h-2 w-2">
                  <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
                  <span className="relative inline-flex rounded-full h-2 w-2 bg-red-500"></span>
                </span>
                <span className="text-xs font-black uppercase tracking-widest">PRETEK PREBIEHA</span>
            </div>
        )
    }
    if (status === 'registration') {
        return (
            <div className="flex items-center gap-2 bg-emerald-500/10 border border-emerald-500/20 text-emerald-500 px-3 py-1 rounded-full">
                <div className="w-2 h-2 bg-emerald-500 rounded-full" />
                <span className="text-xs font-black uppercase tracking-widest">REGISTRÁCIA</span>
            </div>
        )
    }
    return (
        <div className="flex items-center gap-2 bg-slate-500/10 border border-slate-500/20 text-slate-500 px-3 py-1 rounded-full">
            <span className="text-xs font-black uppercase tracking-widest">UKONČENÉ</span>
        </div>
    )
};

const HorizontalBarChart = ({ data }: { data: any[] }) => {
    const max = Math.max(...data.map(d => d.weight));
    return (
        <div className="space-y-4">
            {data.map((d, i) => (
                <div key={i}>
                    <div className="flex justify-between text-xs mb-1">
                        <span className="text-white font-bold">{d.name}</span>
                        <span className="text-slate-400">{d.weight} kg</span>
                    </div>
                    <div className="h-3 bg-slate-800 rounded-full overflow-hidden">
                        <div className={`h-full ${d.color}`} style={{ width: `${(d.weight / max) * 100}%` }}></div>
                    </div>
                </div>
            ))}
        </div>
    )
};

const VerticalBarChart = ({ data }: { data: any[] }) => {
    const max = Math.max(...data.map(d => d.val));
    return (
        <div className="h-40 flex items-end justify-between gap-2 mt-4">
            {data.map((d, i) => (
                <div key={i} className="flex flex-col items-center flex-1 h-full justify-end group">
                     <div className="relative w-full h-full flex items-end">
                        <div 
                        className={`w-full rounded-t-sm transition-all duration-500 ${d.val === max ? 'bg-amber-500' : 'bg-slate-700 group-hover:bg-slate-600'}`}
                        style={{ height: `${(d.val / max) * 100}%` }}
                        />
                         <div className="absolute -top-8 left-1/2 -translate-x-1/2 bg-black/80 text-white text-[10px] py-1 px-2 rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap z-10">
                            {d.val} ks
                         </div>
                     </div>
                    <span className="text-[10px] text-slate-500 mt-2 font-mono">{d.hour}</span>
                </div>
            ))}
        </div>
    )
};

// --- MAIN LAYOUT ---

export default function CompetitionDetail() {
  const { id } = useParams();
  const [, setLocation] = useLocation();
  const [showStatsOverlay, setShowStatsOverlay] = useState(false);
  const [leaderboardExpanded, setLeaderboardExpanded] = useState(false);

  const isRegistration = competition.status === 'registration';
  const visibleLeaderboard = leaderboardExpanded ? leaderboard : leaderboard.slice(0, 10);

  return (
    <div className="min-h-screen bg-[#050B14] text-slate-200 font-sans pb-24 selection:bg-cyan-500/30">
      
      {/* 1. HEADER */}
      <header className="relative overflow-hidden border-b border-white/5 bg-[#0B1120]">
         <div className="absolute inset-0 opacity-20 pointer-events-none">
             <img src={competition.image} className="w-full h-full object-cover blur-3xl scale-110" />
         </div>
         <div className="absolute inset-0 bg-gradient-to-b from-transparent to-[#050B14]"></div>

         <div className="relative z-10 max-w-7xl mx-auto px-4 md:px-6 pt-8 pb-6">
             <div className="flex flex-col md:flex-row md:items-start justify-between gap-6">
                 <div>
                     <div className="flex items-center gap-3 mb-3">
                        <StatusBadge status={competition.status} />
                        <span className="text-xs text-slate-400 font-mono flex items-center gap-1">
                            <Clock size={12} /> Do konca zostávajú 3 dni
                        </span>
                     </div>
                     <h1 className="text-3xl md:text-5xl font-black text-white tracking-tight leading-none mb-2">
                        {competition.name}
                     </h1>
                     <div className="flex flex-wrap items-center gap-4 text-sm text-slate-400">
                         <span className="flex items-center gap-1.5"><MapPin size={14} className="text-cyan-500" /> {competition.location}</span>
                         <span className="flex items-center gap-1.5"><Users size={14} className="text-emerald-500" /> Na štarte {competition.teamsCount} tímov</span>
                     </div>
                 </div>

                 <div className="flex gap-3">
                     <button className="bg-white/5 hover:bg-white/10 text-white p-3 rounded-xl border border-white/10 transition-colors">
                        <Share2 size={20} />
                     </button>
                     {isRegistration ? (
                         <button className="bg-emerald-600 hover:bg-emerald-500 text-white px-8 py-3 rounded-xl font-bold shadow-lg shadow-emerald-900/20 flex items-center gap-2 transition-all hover:scale-105">
                            <Users size={20} />
                            <span>Registrovať Tím</span>
                         </button>
                     ) : (
                         <button className="bg-cyan-600 hover:bg-cyan-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-cyan-900/20 flex items-center gap-2 transition-all">
                            <Target size={20} />
                            <span>Môj Tím</span>
                         </button>
                     )}
                 </div>
             </div>
         </div>
      </header>

      {/* 2. STICKY STATS BAR */}
      {!isRegistration && (
          <div className="border-b border-white/5 bg-[#0B1120]/80 backdrop-blur-md sticky top-0 z-40">
              <div className="max-w-7xl mx-auto px-4 py-3 overflow-x-auto no-scrollbar">
                  <div className="flex gap-6 min-w-max md:w-full md:grid md:grid-cols-4 md:gap-0">
                      <div className="flex items-center gap-3 px-2">
                          <div className="p-1.5 bg-cyan-500/10 rounded-lg text-cyan-500"><Fish size={16} /></div>
                          <div>
                              <div className="text-[10px] text-slate-500 uppercase font-bold">Ulovili spolu</div>
                              <div className="text-lg font-black text-white leading-none">{liveStats.totalFish} <span className="text-xs font-normal text-slate-500">ks</span></div>
                          </div>
                      </div>
                      <div className="flex items-center gap-3 px-2 md:border-l border-white/5">
                          <div className="p-1.5 bg-emerald-500/10 rounded-lg text-emerald-500"><Activity size={16} /></div>
                          <div>
                              <div className="text-[10px] text-slate-500 uppercase font-bold">Váha úlovkov</div>
                              <div className="text-lg font-black text-white leading-none">{liveStats.totalWeight} <span className="text-xs font-normal text-slate-500">kg</span></div>
                          </div>
                      </div>
                      <div className="flex items-center gap-3 px-2 md:border-l border-white/5">
                          <div className="p-1.5 bg-amber-500/10 rounded-lg text-amber-500"><Trophy size={16} /></div>
                          <div>
                              <div className="text-[10px] text-slate-500 uppercase font-bold">Najväčšia ryba</div>
                              <div className="text-lg font-black text-white leading-none">{liveStats.biggestFish} <span className="text-xs font-normal text-slate-500">kg</span></div>
                          </div>
                      </div>
                      <div className="flex items-center gap-3 px-2 md:border-l border-white/5">
                          <div className="p-1.5 bg-purple-500/10 rounded-lg text-purple-500"><TrendingUp size={16} /></div>
                          <div>
                              <div className="text-[10px] text-slate-500 uppercase font-bold">Priemerná váha</div>
                              <div className="text-lg font-black text-white leading-none">{liveStats.avgWeight} <span className="text-xs font-normal text-slate-500">kg</span></div>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      )}

      {/* 3. MAIN CONTENT GRID */}
      <main className="max-w-7xl mx-auto px-4 md:px-6 py-8">
         {isRegistration ? (
             /* REGISTRATION MODE */
             <div className="max-w-3xl mx-auto text-center space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-500">
                 <div className="p-8 rounded-3xl bg-[#111a2e] border border-white/5 relative overflow-hidden">
                     <div className="absolute top-0 right-0 w-64 h-64 bg-emerald-500/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2 pointer-events-none"></div>
                     <Timer className="w-16 h-16 text-emerald-500 mx-auto mb-4" />
                     <h2 className="text-3xl font-bold text-white mb-2">Registrácia Otvorená</h2>
                     <button className="w-full bg-emerald-600 hover:bg-emerald-500 text-white py-4 rounded-xl font-bold text-lg shadow-xl shadow-emerald-900/20 transition-all">
                         Vyplniť Prihlášku
                     </button>
                 </div>
             </div>
         ) : (
             /* LIVE MATCH CENTER MODE */
             <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
                
                {/* --- LEFT COLUMN: LEADERBOARD & PODIUM (8/12) --- */}
                <div className="lg:col-span-8 space-y-8">
                    
                    {/* PODIUM */}
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4 items-end">
                         {/* 2nd Place */}
                         <div className="order-2 md:order-1 bg-[#111a2e] border border-white/5 rounded-2xl p-4 flex flex-col items-center justify-end h-40 md:h-48 relative mt-4 md:mt-0">
                             <div className="absolute -top-4 w-10 h-10 bg-slate-700 rounded-full flex items-center justify-center font-bold text-white border-4 border-[#050B14] shadow-lg">2</div>
                             <div className="text-center w-full">
                                 <div className="font-bold text-white mb-1 truncate px-2">{leaderboard[1].name}</div>
                                 <div className="text-2xl font-black text-slate-300">{leaderboard[1].weight}</div>
                                 <div className="text-xs text-slate-500 font-mono">{leaderboard[1].fish} rýb</div>
                             </div>
                         </div>

                         {/* Winner */}
                         <div className="order-1 md:order-2 bg-gradient-to-b from-[#111a2e] to-[#0f172a] border border-amber-500/30 rounded-2xl p-4 flex flex-col items-center justify-end h-48 md:h-56 relative shadow-[0_0_30px_rgba(245,158,11,0.1)] z-10">
                             <div className="absolute -top-6 w-14 h-14 bg-amber-500 rounded-full flex items-center justify-center font-black text-black text-xl border-4 border-[#050B14] shadow-lg shadow-amber-500/20">
                                 <Crown size={24} />
                             </div>
                             <div className="text-center w-full mb-2">
                                 <div className="font-bold text-amber-500 mb-1 text-lg px-2 truncate">{leaderboard[0].name}</div>
                                 <div className="text-4xl font-black text-white">{leaderboard[0].weight}</div>
                                 <div className="text-sm text-slate-400 font-mono">{leaderboard[0].fish} rýb</div>
                             </div>
                             <div className="w-full bg-white/5 rounded-lg py-1 text-center text-[10px] text-slate-500 uppercase font-bold tracking-wider">
                                 Lovia v sektore {leaderboard[0].sector}
                             </div>
                         </div>

                         {/* 3rd Place */}
                         <div className="order-3 md:order-3 bg-[#111a2e] border border-white/5 rounded-2xl p-4 flex flex-col items-center justify-end h-40 md:h-48 relative mt-4 md:mt-0">
                             <div className="absolute -top-4 w-10 h-10 bg-orange-800 rounded-full flex items-center justify-center font-bold text-white border-4 border-[#050B14] shadow-lg">3</div>
                             <div className="text-center w-full">
                                 <div className="font-bold text-white mb-1 truncate px-2">{leaderboard[2].name}</div>
                                 <div className="text-2xl font-black text-orange-200/60">{leaderboard[2].weight}</div>
                                 <div className="text-xs text-slate-500 font-mono">{leaderboard[2].fish} rýb</div>
                             </div>
                         </div>
                    </div>

                    {/* LEADERBOARD TABLE */}
                    <div className="bg-[#111a2e] border border-white/5 rounded-3xl overflow-hidden shadow-sm">
                        <div className="p-5 border-b border-white/5 flex justify-between items-center bg-[#0B1120]">
                            <h3 className="font-bold text-white flex items-center gap-2">
                                <Trophy size={16} className="text-slate-500" />
                                Priebežné poradie tímov
                            </h3>
                        </div>
                        <div className="overflow-x-auto">
                            <table className="w-full text-sm text-left">
                                <thead className="text-xs text-slate-500 uppercase bg-black/20 font-bold tracking-wider">
                                    <tr>
                                        <th className="px-6 py-4 w-16">#</th>
                                        <th className="px-6 py-4">Tím</th>
                                        <th className="px-6 py-4 text-center">Sektor</th>
                                        <th className="px-6 py-4 text-right">Ryby</th>
                                        <th className="px-6 py-4 text-right">Váha spolu (kg)</th>
                                    </tr>
                                </thead>
                                <tbody className="divide-y divide-white/5">
                                    {visibleLeaderboard.map((team, index) => (
                                        <tr key={index} className={`hover:bg-white/[0.02] transition-colors ${index < 3 ? 'bg-white/[0.01]' : ''}`}>
                                            <td className={`px-6 py-4 font-mono font-bold ${index === 0 ? 'text-amber-500' : index === 1 ? 'text-slate-300' : index === 2 ? 'text-orange-400' : 'text-slate-500'}`}>
                                                {team.rank}.
                                            </td>
                                            <td className="px-6 py-4 font-bold text-white">{team.name}</td>
                                            <td className="px-6 py-4 text-center text-slate-500">{team.sector}</td>
                                            <td className="px-6 py-4 text-right text-slate-400 font-mono">{team.fish}</td>
                                            <td className="px-6 py-4 text-right font-black text-white text-base">{team.weight}</td>
                                        </tr>
                                    ))}
                                </tbody>
                            </table>
                        </div>
                        
                        <div className="p-2 bg-[#0B1120] border-t border-white/5 lg:hidden">
                            <button 
                                onClick={() => setLeaderboardExpanded(!leaderboardExpanded)}
                                className="w-full py-3 flex items-center justify-center gap-2 text-slate-400 text-xs font-bold uppercase tracking-widest hover:text-white transition-colors"
                            >
                                {leaderboardExpanded ? (
                                    <>Menej <ChevronUp size={14} /></>
                                ) : (
                                    <>Celá Tabuľka <ChevronDown size={14} /></>
                                )}
                            </button>
                        </div>
                    </div>
                </div>

                {/* --- RIGHT COLUMN: FEED & INFO (4/12) --- */}
                <div className="lg:col-span-4 space-y-6">
                    
                    {/* 5.1 THE COMMENTATOR TEASER (NEW) */}
                    <div className="bg-[#111a2e] border border-white/5 rounded-3xl p-6 relative overflow-hidden group hover:border-blue-500/30 transition-colors">
                        <div className="absolute top-0 right-0 p-20 bg-blue-500/10 blur-3xl rounded-full group-hover:bg-blue-500/20 transition-colors pointer-events-none"></div>
                        
                        <div className="relative z-10">
                            {/* NEW: Commentator Label */}
                            <div className="flex items-center gap-2 mb-3 text-amber-400">
                                <Mic size={14} className="animate-pulse" />
                                <span className="text-[10px] font-bold uppercase tracking-widest">Komentár z dát</span>
                            </div>
                            
                            {/* NEW: Short Commentary */}
                            <p className="text-white font-bold text-lg leading-tight mb-6">
                                "{getCommentary('short')}"
                            </p>

                            <button 
                                onClick={() => setShowStatsOverlay(true)}
                                className="w-full bg-blue-600 hover:bg-blue-500 text-white py-3.5 rounded-xl font-bold shadow-lg shadow-blue-900/20 transition-all flex items-center justify-center gap-2"
                            >
                                <PieChart size={16} />
                                Pozrieť, kde a kedy berú ryby
                            </button>
                        </div>
                    </div>

                    {/* 5.2 LIVE FEED */}
                    <div className="bg-[#111a2e] border border-white/5 rounded-3xl overflow-hidden flex flex-col max-h-[600px]">
                        <div className="p-4 border-b border-white/5 bg-[#0B1120] flex items-center justify-between sticky top-0 z-10">
                            <h3 className="font-bold text-white text-sm uppercase tracking-wider">Čo sa deje pri vode</h3>
                            <span className="flex items-center gap-1.5 text-[10px] text-emerald-500 font-bold uppercase bg-emerald-500/10 px-2 py-1 rounded-full">
                                <span className="relative flex h-1.5 w-1.5">
                                  <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                                  <span className="relative inline-flex rounded-full h-1.5 w-1.5 bg-emerald-500"></span>
                                </span>
                                Online
                            </span>
                        </div>
                        
                        <div className="p-4 space-y-6 overflow-y-auto custom-scrollbar">
                            {liveFeed.map((item) => (
                                <div key={item.id} className={`relative pl-4 ${item.system ? 'opacity-60' : ''}`}>
                                    <div className="absolute left-0 top-3 bottom-[-24px] w-[2px] bg-white/5 last:hidden"></div>
                                    <div className={`absolute left-[-3px] top-3 w-2 h-2 rounded-full border border-[#111a2e] ${item.action === 'big_fish' ? 'bg-amber-500' : item.system ? 'bg-slate-600' : 'bg-cyan-500'}`}></div>

                                    {item.system ? (
                                        <div className="text-xs">
                                            <div className="font-bold text-slate-300 mb-0.5">INFO Z PRETEKU</div>
                                            <p className="text-slate-500 italic">{item.msg}</p>
                                        </div>
                                    ) : (
                                        <div>
                                            <div className="flex justify-between items-start mb-1">
                                                <span className="text-xs font-bold text-white truncate max-w-[140px]">{item.team}</span>
                                                <span className="text-[10px] text-slate-600 font-mono">{item.time}</span>
                                            </div>
                                            <div className="flex items-center gap-2 bg-[#050B14] p-2 rounded-lg border border-white/5">
                                                {item.action === 'big_fish' ? (
                                                    <div className="flex items-center gap-2 w-full">
                                                        <Crown size={14} className="text-amber-500 shrink-0" />
                                                        <span className="text-sm text-amber-500 font-bold">
                                                            Padla veľká ryba! <span className="text-white font-black ml-1">{item.weight} kg</span>
                                                        </span>
                                                    </div>
                                                ) : (
                                                    <div className="text-sm text-slate-400 w-full flex justify-between items-center">
                                                        <span>{item.fish}</span>
                                                        <span className="text-white font-black">{item.weight} kg</span>
                                                    </div>
                                                )}
                                            </div>
                                        </div>
                                    )}
                                </div>
                            ))}
                        </div>
                    </div>

                    {/* 5.3 MAP & RULES */}
                    <div className="grid grid-cols-2 gap-3">
                        <button className="p-4 rounded-2xl bg-[#111a2e] border border-white/5 hover:bg-white/5 transition-colors text-left group">
                            <MapPin size={20} className="text-slate-500 group-hover:text-cyan-500 mb-2 transition-colors" />
                            <div className="text-sm font-bold text-white">Kde kto loví</div>
                            <div className="text-[10px] text-slate-500">Mapa sektorov</div>
                        </button>
                        <button className="p-4 rounded-2xl bg-[#111a2e] border border-white/5 hover:bg-white/5 transition-colors text-left group">
                            <AlertCircle size={20} className="text-slate-500 group-hover:text-cyan-500 mb-2 transition-colors" />
                            <div className="text-sm font-bold text-white">Ako sa boduje</div>
                            <div className="text-[10px] text-slate-500">Čo platí na tomto preteku</div>
                        </button>
                    </div>

                </div>
             </div>
         )}
      </main>

      {/* --- TACTICAL STATS OVERLAY (MODAL) --- */}
      {showStatsOverlay && (
          <div className="fixed inset-0 z-50 flex items-center justify-center p-4 md:p-8 animate-in fade-in duration-200">
              {/* Backdrop */}
              <div 
                className="absolute inset-0 bg-black/95 backdrop-blur-sm"
                onClick={() => setShowStatsOverlay(false)}
              ></div>

              {/* Modal Content */}
              <div className="relative z-10 bg-[#0f172a] border border-white/10 w-full max-w-5xl max-h-[90vh] rounded-[32px] overflow-hidden flex flex-col shadow-2xl animate-in zoom-in-95 duration-200">
                  
                  {/* Modal Header */}
                  <div className="p-6 md:p-8 border-b border-white/5 flex items-center justify-between bg-[#111a2e]">
                      <div>
                          <h2 className="text-xl md:text-2xl font-black text-white flex items-center gap-3">
                              <PieChart className="text-blue-500" />
                              Ako ryby berú počas preteku
                          </h2>
                          <p className="text-slate-400 text-sm mt-1">Zábery a úlovky bez omáčky</p>
                      </div>
                      <button 
                        onClick={() => setShowStatsOverlay(false)}
                        className="p-3 bg-white/5 hover:bg-white/10 rounded-full transition-colors text-white"
                      >
                          <X size={24} />
                      </button>
                  </div>

                  {/* Modal Scrollable Body */}
                  <div className="flex-1 overflow-y-auto p-6 md:p-8 space-y-8 custom-scrollbar bg-[#050B14]">
                      
                      {/* NEW: Full Commentator Block */}
                      <div className="bg-blue-500/10 border border-blue-500/20 p-4 md:p-6 rounded-2xl flex gap-4 items-start">
                           <div className="shrink-0 w-10 h-10 bg-blue-500/20 rounded-full flex items-center justify-center text-blue-400 mt-1">
                                <Mic size={20} />
                           </div>
                           <div>
                               <h4 className="text-xs font-bold text-blue-400 uppercase tracking-wider mb-2">Komentár k preteku</h4>
                               <p className="text-white text-lg md:text-xl font-medium leading-relaxed">
                                  "{getCommentary('full')}"
                               </p>
                           </div>
                      </div>

                      {/* Grid for Charts */}
                      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
                          
                          {/* Sector Performance */}
                          <div className="bg-[#111a2e] p-6 rounded-3xl border border-white/5">
                              <h3 className="text-lg font-bold text-white mb-6 flex items-center gap-2">
                                  <MapPin size={18} className="text-emerald-500" />
                                  Kde sa teraz oplatí loviť
                              </h3>
                              <HorizontalBarChart data={sectorStats} />
                              <p className="text-xs text-slate-500 mt-6 leading-relaxed bg-black/20 p-3 rounded-lg">
                                  V Zátoke sa chytajú ťažšie ryby. Na Hrádzi je viac záberov, ale menšie kusy.
                              </p>
                          </div>

                          {/* Hourly Activity */}
                          <div className="bg-[#111a2e] p-6 rounded-3xl border border-white/5">
                              <h3 className="text-lg font-bold text-white mb-2 flex items-center gap-2">
                                  <Clock size={18} className="text-amber-500" />
                                  Kedy prichádzajú zábery
                              </h3>
                              <p className="text-xs text-slate-400 mb-6">Časy, kedy sa ryby najčastejšie hlásia</p>
                              <VerticalBarChart data={hourlyActivity} />
                          </div>
                      </div>

                      {/* Additional Stats Row */}
                      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                          <div className="bg-[#111a2e] p-4 rounded-2xl text-center border border-white/5">
                              <div className="text-[10px] text-slate-500 uppercase font-bold mb-1 tracking-wider">Priemerná veľkosť úlovku</div>
                              <div className="text-2xl font-black text-white">3.6 kg</div>
                          </div>
                          <div className="bg-[#111a2e] p-4 rounded-2xl text-center border border-white/5">
                              <div className="text-[10px] text-slate-500 uppercase font-bold mb-1 tracking-wider">Tímy zatiaľ bez ryby</div>
                              <div className="text-lg font-black text-white">5 tímov</div>
                              <div className="text-[10px] text-slate-500">zatiaľ bez ryby</div>
                          </div>
                          <div className="bg-[#111a2e] p-4 rounded-2xl text-center border border-white/5">
                              <div className="text-[10px] text-slate-500 uppercase font-bold mb-1 tracking-wider">Najväčší amur</div>
                              <div className="text-2xl font-black text-white">12.5 kg</div>
                          </div>
                          <div className="bg-[#111a2e] p-4 rounded-2xl text-center border border-white/5">
                              <div className="text-[10px] text-slate-500 uppercase font-bold mb-1 tracking-wider">Najčastejšie veľkosti</div>
                              <div className="text-2xl font-black text-white">2 - 4 kg</div>
                          </div>
                      </div>

                  </div>
              </div>
          </div>
      )}

    </div>
  );
}

